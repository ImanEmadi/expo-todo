import { ToastOptions } from "react-native-root-toast";

export const DEFAULT_TOAST_OPTIONS: ToastOptions = {
    duration: 5000,
    position: -80,
    animation: true,
    shadow: true,
    delay: 0,
    hideOnPress: true,
    opacity: 1
}